# Stanley Racing Prediction System - Environment Variables Template
# Copy this file to .env and fill in your actual credentials

# ============================================================================
# EMAIL CONFIGURATION (for sending prediction CSVs)
# ============================================================================
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
ALERT_EMAIL=recipient@example.com

# Gmail App Password Instructions:
# 1. Enable 2FA on your Google account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Generate an app password for "Mail"
# 4. Use that 16-character password (no spaces) as SMTP_PASSWORD

# ============================================================================
# TWILIO SMS CONFIGURATION (for sending race predictions)
# ============================================================================
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_FROM_PHONE=+1234567890

# Twilio Setup Instructions:
# 1. Sign up at https://www.twilio.com/
# 2. Get a phone number (may require $$ or use trial)
# 3. Copy Account SID and Auth Token from dashboard
# 4. Note: SMS costs apply (trial has free credits)

# ============================================================================
# ALERT PHONE (hardcoded in scripts/scheduler.py line 29)
# ============================================================================
# The recipient phone number is currently hardcoded in scheduler.py:
# ALERT_PHONE = "+61417676973"
# 
# To change it, edit scripts/scheduler.py line 29

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Use PostgreSQL instead of SQLite (set to 'true' to enable)
USE_POSTGRES=false

# PostgreSQL connection string (required when USE_POSTGRES=true)
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Optional: SQLite database paths (defaults are fine when USE_POSTGRES=false)
# DB_PATH=data/historical/hkjc.db
# SCHEDULER_DB_PATH=data/historical/scheduler_jobs.db

# ============================================================================
# WEB APPLICATION CONFIGURATION
# ============================================================================
# JWT Secret Key for authentication (IMPORTANT: Change this in production!)
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=change-this-to-a-random-secret-key-in-production

# ============================================================================
# NOTES
# ============================================================================
# - NEVER commit .env to git (it's in .gitignore)
# - Store securely on server (chmod 600 .env)
# - For DigitalOcean, can also use environment variables in droplet settings

